# name: Shareify CI – Build & Push Docker Images

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# env:
#   DOCKER_NAMESPACE: shareify

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         service:
#           - api-gateway
#           - user-service
#           - item-service
#           - inventory-service
#           - booking-service
#           - payment-service
#           - review-service

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to Docker Hub
#         if: github.event_name == 'push'
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Build and Push – shareify-${{ matrix.service }}
#         uses: docker/build-push-action@v5
#         with:
#           context: ./${{ matrix.service }}
#           push: ${{ github.event_name == 'push' }}
#           tags: |
#             ${{ env.DOCKER_NAMESPACE }}/shareify-${{ matrix.service }}:latest
#             ${{ env.DOCKER_NAMESPACE }}/shareify-${{ matrix.service }}:${{ github.sha }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
