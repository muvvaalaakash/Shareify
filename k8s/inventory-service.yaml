apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: shareify-inventory-service
  labels:
    app: shareify-inventory-service
spec:
  serviceName: "shareify-inventory-service"
  replicas: 1
  selector:
    matchLabels:
      app: shareify-inventory-service
  template:
    metadata:
      labels:
        app: shareify-inventory-service
    spec:
      containers:
        - name: inventory-service
          image: mvnsakash/shareify-inventory-service:v1.1.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_PATH
              value: "/data/inventory.db"
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: "csi-standard"
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: shareify-inventory-service
  labels:
    app: shareify-inventory-service
spec:
  type: ClusterIP
  selector:
    app: shareify-inventory-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
